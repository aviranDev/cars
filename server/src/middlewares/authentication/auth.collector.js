import token from './auth.token.js';
import credentials from './auth.credentials.js';
import existenceUser from './auth.existanceUser.js';

const authenticate = {
  token,
  credentials,
  existenceUser
};

export default authenticate;